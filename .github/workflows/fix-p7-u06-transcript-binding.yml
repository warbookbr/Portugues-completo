name: Fix P7 U06 transcript binding

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'p7-n0-u06-catalog-v2' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: p7-n0-u06-catalog-v2
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false
      - name: Apply binding fix
        run: python3 scripts/fix-p7-u06-transcript-binding.py
      - name: Validate U06 semantics
        run: |
          node scripts/audit-p7-n0-u06.mjs
          node scripts/test-p7-u06-communication.mjs
          node scripts/test-classic-renderer.mjs
      - name: Commit fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/js/ui/classic-renderer.js scripts/test-p7-u06-communication.mjs
          git commit -m 'fix(p7): corrige binder de transcrição da U06'
          git push origin HEAD:p7-n0-u06-catalog-v2
