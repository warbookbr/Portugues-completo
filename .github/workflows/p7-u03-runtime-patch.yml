name: Apply P7 U03 patch (temporary, disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Patches U03 já aplicados; workflow temporário será removido antes do merge.'
