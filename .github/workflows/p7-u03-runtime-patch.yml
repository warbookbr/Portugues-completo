name: Apply P7 U03 runtime patch (temporary, disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Patch já aplicado; este workflow será removido antes do merge.'
